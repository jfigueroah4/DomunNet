{
  "name": "domun-control-obra",
  "version": "1.0.0",
  "description": "Monorepo para sistema de control de obras",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --no-daemon",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "clean": "turbo run clean && node -e \"const fs=require('fs'); for (const path of ['node_modules','.turbo']) fs.rmSync(path,{recursive:true,force:true})\""
  },
  "devDependencies": {
    "turbo": "^1.10.16",
    "prettier": "^3.1.0"
  }
}
